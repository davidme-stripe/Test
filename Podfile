# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Test' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Test
  # pod 'Stripe', '~> 16.0.0'
  pod 'Stripe', :git => 'https://github.com/davidme-stripe/stripe-ios.git', :branch=>'davidme/3ds2-static'
  pod 'Alamofire', '~> 4.8.2'

end
